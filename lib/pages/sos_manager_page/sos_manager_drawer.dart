import 'package:flutter/material.dart';

class SosManagerDrawer extends StatelessWidget {
  const SosManagerDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.white,);
  }
}